"""Retrieval-augmented generation: embeddings, ingestion, and search.

The knowledge base is hand-authored, claim-sized passages with citations.
This package turns them into vectors and finds the relevant ones for a
question. The LLM (elsewhere) only explains what this package retrieves.
"""
